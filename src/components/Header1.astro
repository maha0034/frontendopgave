<header>
  <nav>
    <a href="/" class="logo">
      Ask<span class="gul">Experts</span>
    </a>

    <div class="inner-menu">
      <a class="inner-header" href="">Home</a>
      <a class="inner-header" href="">Case Study</a>
      <a class="inner-header" href="">Team</a>
      <a class="inner-header" href="">About</a>
    </div>

    <div class="login-wrapper">
      <button class="popover" type="button">Login</button>

      <div id="menu">
        <h2 class="login-title">Dashboard Login</h2>

        <form class="login-form">
          <label for="email">Email address</label>
          <input type="email" id="email" />

          <label for="password">Password</label>
          <input type="password" id="password" />

          <button type="submit" class="dashboard-btn">Go to dashboard</button>
        </form>
      </div>
    </div>
  </nav>
</header>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Cabin:ital,wght@0,400..700;1,400..700&display=swap");
  @import url("https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&display=swap");

  :global(body) {
    margin: 0;
  }

  header {
    width: 100%;
    background: var(--color-neutrals-900, #020814);
    display: flex;
    justify-content: center;
    align-items: flex-start;
    padding: 38px 0;
    backdrop-filter: blur(10.5px);
  }

  nav {
    width: min(1200px, calc(100% - 48px));
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    column-gap: 48px;
    row-gap: 0;
    margin: 0 auto;
  }

  .inner-menu {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 30px;
    flex-wrap: wrap;
  }

  .logo {
    color: white;
    font-family: "Cabin", sans-serif;
    font-size: 30px;
    font-style: normal;
    font-weight: 600;
    line-height: 105%;
    text-decoration: none;
  }

  .gul {
    color: #ffd700;
  }

  .inner-header {
    color: white;
    font-family: "Lato", sans-serif;
    font-size: 17.728px;
    font-style: normal;
    font-weight: 400;
    line-height: 160%;
    text-decoration: none;
  }

  .popover {
    display: flex;
    padding: 4px 16px;
    justify-content: center;
    align-items: center;
    gap: 10px;
    align-self: stretch;
    border-radius: 999px;
    background-color: white;
    border: none;
  }

  .login-wrapper {
    position: relative;
    display: inline-block;
  }

  #menu {
    display: none;
    position: absolute;
    top: calc(100% + 12px);
    right: 0;
    z-index: 1000;

    border: none;
    margin: 0;
    padding: 32px;
    width: 390px;
    max-width: calc(100vw - 32px);
    border-radius: 20px;
    background: #f4f4f4;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.18);
  }

  .login-wrapper:focus-within #menu {
    display: block;
  }

  .login-title {
    margin: 0 0 32px 0;
    color: #111;
    font-family: "Cabin", sans-serif;
    font-size: 30px;
    font-weight: 700;
    line-height: 1.1;
  }

  .login-form {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .login-form label {
    color: #222;
    font-family: "Lato", sans-serif;
    font-size: 16px;
    font-weight: 500;
  }

  .login-form input {
    width: 100%;
    height: 56px;
    border-radius: 999px;
    border: 1.5px solid #cfcfcf;
    background: #f4f4f4;
    padding: 0 20px;
    font-size: 16px;
    outline: none;
    box-sizing: border-box;
    margin-bottom: 10px;
  }

  .login-form input:focus {
    border-color: #111;
  }

  .dashboard-btn {
    margin-top: 12px;
    width: 100%;
    height: 58px;
    border: none;
    border-radius: 999px;
    background: #f6c843;
    color: #111;
    font-family: "Lato", sans-serif;
    font-size: 18px;
    font-weight: 700;
    cursor: pointer;
  }
</style>
